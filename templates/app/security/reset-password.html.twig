{% extends 'base.html.twig' %}

{% block body %}
    <div class="login-page">

        <div class="form">
            {{ form_start(form) }}
                {{ form_row(form.password.first) }}
                {{ form_row(form.password.second) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
